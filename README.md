# vendor_oneplus_camera-macan

OnePlus 15R (macan) per-device camera source contract. `camera-macan.mk`
inherits `camera-sm8850-common.mk`, while `proprietary-files.txt` and
`extract-files.py` drive the paired generated repo:
`proprietary/vendor/oneplus/camera-macan`.

- Build path: `vendor/oneplus/camera-macan`
