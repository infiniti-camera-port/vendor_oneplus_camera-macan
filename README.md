# vendor_oneplus_camera-macan

OnePlus 15R (macan) per-device camera vendor config: `camera-macan.mk`
inherits `camera-sm8850-common.mk` and leaves payload population to the
matching proprietary camera repo.

- Build path: `vendor/oneplus/camera-macan`
